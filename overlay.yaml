overlay: 1.0.0
x-speakeasy-jsonpath: rfc9535
info:
  title: Overlay original_modified.yaml => original.yaml
  version: 0.0.0
actions:
  - target: $["tags"][*]
    remove: true
  - target: $["tags"]
    update:
      - name: webhooks
        description: Configure and trigger webhooks
      - name: example
        description: Generate example payloads for webhooks
  - target: $["paths"]["/v2/webhooks/configs/{configId}/events"]["post"]["tags"][0]
    update: events
